{
  "name": "@flxlfx/changelog-cli",
  "version": "0.2.0",
  "type": "module",
  "private": false,
  "bin": {
    "jc-cli": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "module": "./src/index.ts",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bun build src/cli.ts --target=node --outdir=dist",
    "dev": "bun run src/cli.ts",
    "cli": "bun run dist/cli.js",
    "setup-hooks": "chmod +x .git/hooks/post-checkout",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "bun-types": "^1.3.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.0.1",
    "zod": "^4.1.13"
  }
}
